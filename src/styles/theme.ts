import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1FA542',
    },
  },
});

export const colors = {
  green: '#1FA542',
  white: '#fff',
  yellow: '#yellow',
  red: '#E0201F',
  blue: '#036AB0',
  Brown: '#D19362',
  black: '#000',
  gray: '#ddd',
  lightgrey: '#ccc',
};

export default theme;
